<Window x:Class="MacEstimator.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MacEstimator.App"
        Title="Defaults — Line Items &amp; Rates"
        Width="800" Height="600"
        WindowStartupLocation="CenterOwner"
        Icon="pack://application:,,,/Assets/app.ico">

    <Window.Resources>
        <!-- DataGrid cell style -->
        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource BackgroundSelected}"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid row style -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{StaticResource BackgroundMedium}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource BackgroundSelected}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BackgroundHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid column header style -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource BackgroundDarkest}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundSecondary}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="16">
        <!-- Header -->
        <TextBlock DockPanel.Dock="Top"
                   Text="DEFAULT LINE ITEMS &amp; RATES"
                   FontWeight="Bold"
                   FontSize="16"
                   Foreground="{StaticResource AccentBrush}"
                   Margin="0,0,0,4"/>
        <TextBlock DockPanel.Dock="Top"
                   Text="Changes here apply to all new estimates for everyone."
                   Foreground="{StaticResource ForegroundSecondary}"
                   FontSize="11"
                   Margin="0,0,0,12"/>

        <!-- Bottom buttons -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Add Item"
                    Style="{StaticResource GhostButtonStyle}"
                    Padding="12,8"
                    Click="OnAddItem"
                    Margin="0,0,8,0"/>
            <Button Content="Save"
                    Style="{StaticResource AccentButtonStyle}"
                    Padding="16,8"
                    Click="OnSaveClick"
                    Margin="0,0,8,0"/>
            <Button Content="Cancel"
                    Style="{StaticResource GhostButtonStyle}"
                    Padding="16,8"
                    Click="OnCancelClick"/>
        </StackPanel>

        <!-- Line items grid -->
        <DataGrid x:Name="ItemsGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="True"
                  Background="{StaticResource BackgroundDark}"
                  Foreground="{StaticResource ForegroundPrimary}"
                  BorderBrush="{StaticResource BorderBrush}"
                  BorderThickness="1"
                  RowBackground="{StaticResource BackgroundMedium}"
                  AlternatingRowBackground="{StaticResource BackgroundDark}"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                  HeadersVisibility="Column"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Rate" Binding="{Binding DefaultRate, StringFormat=0.##}" Width="80"/>
                <DataGridComboBoxColumn Header="Unit" Width="100"
                                        SelectedItemBinding="{Binding Unit}"
                                        ItemsSource="{Binding Source={x:Static local:SettingsWindow.UnitOptions}}"/>
                <DataGridComboBoxColumn Header="Pricing" Width="130"
                                        SelectedItemBinding="{Binding Mode}"
                                        ItemsSource="{Binding Source={x:Static local:SettingsWindow.ModeOptions}}"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
